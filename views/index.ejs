<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link href="/stylesheets/style.css" rel="stylesheet" type="text/css">
  <title></title>
</head>
<body>
  <% if (user) {%>
    <% if (user.membership === "non-member") {%>
      <h1>WELCOME BACK <%= user.first_name %> non-member</h1>
      <a href="/logout">LOG OUT</a>
    <% } %>
    <% if (user.membership === "member") {%>
      <h1>WELCOME BACK <%= user.first_name %> you are a member</h1>
      <a href="/logout">LOG OUT</a>
    <% } %>
  <% } else { %>
    <h1>please log in</h1>
    <h2 class="incorrect-password"></h2>
    <form action="/login" method="POST">
      <label for="username">Username</label>
      <input name="username" placeholder="username" type="text" />
      <label for="password">Password</label>
      <input name="password" type="password" />
      <button>Log In</button>
    </form>
    <h2>Not signed up:</h2>
    <a href="/signup_form">Sign up here:</a>
  <%}%>
</body>
</html>